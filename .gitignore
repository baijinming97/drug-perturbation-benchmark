# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# OS / editor
.DS_Store
*.swp
*.swo
*~
.vscode/
.idea/

# Environment / secrets
.env
.env.local
.envrc

# Generated by fetch_upstream.sh — reviewer rebuilds locally from upstream repos
# (pristine git clones at pinned commits; reproduced by `bash reference/fetch_upstream.sh`)
reference/CIGER/
reference/DeepCE/
reference/MultiDCP/
reference/PertDiT/
reference/PRnet/
reference/TranSiGen/
reference/XPert/
# All per-model data directories — populated by `bash reference/fetch_upstream.sh`
# (CIGER + DeepCE: cp -r from reference/<M>/<M>/data/.)
# (MultiDCP / PertDiT / PRnet / TranSiGen / XPert: zenodo / Tsinghua / Figshare / GitHub Release downloads)
data/CIGER/
data/DeepCE/
data/MultiDCP/
data/PertDiT/
data/PRnet/
data/TranSiGen/
data/XPert/

# Generated by prepare.py — reviewer rebuilds locally
# (extracted from data/XPert/processed_data.zip)
data/XPert/processed_data/
# (symlink to reference/XPert/HG_data)
HG_data
# (P6/P7 model-specific h5ad conversions)
data/_converted/
# (XPert zero/shuffle drug arrays)
data/_xpert_ablation/
# (5-gene HGNC remap, regenerated)
data/CIGER/gene_feature_nm.csv
# (unrar'd lincs_l1000.h5ad)
data/PertDiT/extracted/
# (MolT5+BioLinkBERT embeddings, ~25 min GPU)
data/PertDiT/nm_drug_emb/
data/PertDiT/bench_drug_emb/
# (data.tar.gz extraction, ~30 GB)
data/MultiDCP/extracted/
# (XPert Figshare downloads — large zips + intermediate h5ad)
data/XPert/figshare_28955141/
data/XPert/all_drugs_unimol_arr.npy
data/XPert/l1000_mdmt_full_336852.h5ad
data/XPert/l1000_mdmt_268022_pretrain.zip
data/XPert/saved_model.zip
data/XPert/XPert.zip
# (split CSVs, regenerated by data_prep)
splits/
data/splits/

# Training outputs (reviewer regenerates)
results/
# (local 1-epoch verification artefacts)
test/
# (XPert shim temp YAMLs — best-effort cleaned, seatbelt)
models/XPert/configs/_tmp_*.yaml
# (XPert default expt dir — we use --expt_dir)
models/XPert/experiment/
models/XPert/logs/

# Bulk data — too large for git, hosted on Zenodo / Figshare / GitHub Releases
*.h5
*.h5ad
*.npy
*.npz
*.pkl
*.pickle
*.tar.gz
*.tar
*.zip
*.rar
*.gctx

# Logs / setup transcripts
*.log
*.out
*.err
_setup_logs/
# (regenerated on first install_pip.sh run)
environment/INSTALL_LOG.md
